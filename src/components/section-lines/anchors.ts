/**
 * Shared x-anchors (in the common 620-wide viewBox units from
 * useMeasuredViewBox) that every section's line art hands lines off at, so
 * a line exiting one section's bottom edge and entering the next section's
 * top edge lands at the same screen x-coordinate and reads as continuous.
 */
export const LEFT_EDGE_X = 60
export const RIGHT_EDGE_X = 500
